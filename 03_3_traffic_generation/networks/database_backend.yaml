# Copyright (C) 2025 TU Dresden
#
# Distributed under terms of the MIT license.

networks:
  database_backend:
    enable_ipv4: false
    enable_ipv6: true
    driver: bridge
    ipam:
      config:
        - subnet: ${DATABASE_BACKEND_PREFIX}/64
          gateway: ${DATABASE_BACKEND_PREFIX}1
